version: '3.9'

services:
  app:
    build: .
    container_name: block_quadratic_app
    ports:
      - "8080:80"
    volumes:
      - ./app:/var/www/html
      - data_volume:/var/www/html/data
    restart: always

volumes:
  data_volume:
